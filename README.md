# Receipt Printer
A simple receipt printer implemented in Python.

## Usage
To use the receipt printer, create a `Receipt` object and pass it to the `ReceiptPrinter` class.

## Testing
To run the tests, use the `pytest` command.
